Usage: hadoop jar EdgeDetect.jar 'infolder' 'outfolder'

The program will run a canny edge detector on all images in the infolder and output them in the outfolder.

Dependent on files in the HistEq repository. 
